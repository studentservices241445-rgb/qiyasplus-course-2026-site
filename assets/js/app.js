/*
 * تطبيق JavaScript لموقع دورة قياس بلس.
 * هذا الملف يتعامل مع التنقل، إشعارات الإثبات الاجتماعي، المساعد الافتراضي،
 * نسخ رابط الموقع إلى الحافظة، وعرض آراء الطلاب بالإضافة إلى عداد المقاعد
 * وعدّاد انتهاء الخصم. يمكن تعديل القوائم التالية لتغيير محتوى الآراء أو
 * الإشعارات أو ردود المساعد.
 */

(function () {
  'use strict';

  // ---- مصادر البيانات ----
  // قائمة الآراء (Testimonials). يمكن إضافة المزيد عند الحاجة.
  const reviews = [
    {
      name: 'شهد المالكي',
      quote: 'الخطة خلتني ألتزم وصرت أعرف وش أراجع كل يوم. النصائح اليومية كانت ذهبية!'
    },
    {
      name: 'تركي القحطاني',
      quote: 'فكرة مشاركة الخطة تخلّي الالتزام أسهل. كنت أتردد، لكن يوم شفت الخطة عرفت وين الغلط.'
    },
    {
      name: 'منال التويجري',
      quote: 'التحديد عطاني مسار واضح، والأسئلة القوية مع شرح الإجابات كانت سبب في فهمي.'
    },
    {
      name: 'فهد السبيعي',
      quote: 'الواجهة مريحة، ما فيها زحمة. عطاني القرار الصحيح بسرعة: مكثفة ولا شاملة.'
    },
    {
      name: 'مي القحطاني',
      quote: 'الخطة يوم بيوم خلتني ما أضيع. حتى 15 دقيقة يومياً فرقت معي بشكل كبير.'
    },
    {
      name: 'عبدالله الحربي',
      quote: 'شاركت خطتي مع زميلي وكنا نحمس بعض؛ الالتزام صار أسهل بكثير.'
    },
    {
      name: 'سارة العسيري',
      quote: 'القواعد والاستثناءات كانت أصعب جزء بالنسبة لي، لكن شرح الدورة خلاني أتقنها.'
    }
  ];

  // رسائل الإشعارات. يتم اختيار رسالة عشوائياً ويضاف إليها اسم عشوائي.
  const notificationMessages = [
    'أنهت خطة 7 أيام ✅ وتقول: “التحاسب اليومي فرق معي كثير.”',
    'شارك خطته مع صديقه 📤 — “إذا ما شاركتها ما ألتزم!”',
    'رفع مستواه في القراءة خلال أسبوع 📈 — “صرت أفهم أسرع.”',
    'تقول: “الخطة مرتبة وواضحة.”',
    'تنصح البنات: “شاركي خطتك مع صديقة… الالتزام يزيد.”',
    'يقول: “أفضل شي إن الخطة جاهزة للمشاركة كنص.”',
    'جرب الاختبار وخذ الخطة… بعدين قرر كيف تشترك.',
    'يشيد بالواجهة المريحة ويقول: “ما تحس بضغط.”',
    'تعلم أسلوب الإجابة — مو بس حفظ.',
    'النتيجة مع التحليل تعطيك اتجاه واضح.'
  ];

  // مجموعة الأسماء للإشعارات.
  const namesPool = [
    'تركي القحطاني', 'لمى عبدالله الحربي', 'الهنوف محمد', 'خالد عبدالرحمن', 'شهد المالكي',
    'سهام', 'رياض العنزي', 'هند العنزي', 'شهد الشهري', 'محمد الدوسري', 'تالا العصيمي',
    'حامد مسفر', 'ابراهيم العنزي', 'ريم الحربي', 'نواف الحربي', 'بدر', 'عائشة', 'عمار السلمي',
    'صالحه مجرشي', 'عبدالعزيز الغامدي', 'اسماء العميري', 'لميس محمد', 'انفال الراشد',
    'فيصل عسيري', 'خلود القرني', 'رسيل', 'عبدالله الثقفي', 'عبدالملك', 'محمد آل مرهون',
    'العنود الشهري', 'علي الشهراني', 'أحلام', 'أريام', 'احمد الشمري', 'راما الزهراني',
    'عاد الحربي', 'لمار آل رشيد', 'جوري الغامدي', 'أصيل حكمي', 'مرام المقرن', 'يزيد الرويلي',
    'خلود حسن', 'سكينه الجعفر', 'منى المطيري', 'فاطمه العسيري', 'يوسف', 'سلطان البقمي',
    'سعود البشري', 'عمر', 'بتول الحربي', 'أمجاد المالكي', 'بسام البشري', 'ليلة العنزي',
    'عبدالكريم العدواني', 'سيف هاشم', 'لينا القحطاني', 'رانيا يوسف القرني', 'منيره عبدالله',
    'ياسر ابراهيم', 'شوق جبريل', 'رنيم آل ثابت', 'مشاعل القحطاني', 'مالك', 'لمار العلوي',
    'هاجر العصيمي', 'انور الجهني', 'عبدالرحمن القحطاني', 'حنين', 'جنى', 'فاطمة', 'منيره',
    'أفراح', 'لمى الجهني', 'عبدالملك العتيبي', 'خالد العتيبي', 'سمية راشد'
  ];

  // ردود المساعد الافتراضي بناءً على الكلمات المفتاحية.
  const chatResponses = {
    'دورة': 'هذه دورة مكثفة وشاملة لاجتياز اختبار STEP. يمكنك قراءة التفاصيل في صفحة الدورة أو كتابة سؤالك بالتحديد.',
    'الملفات': 'ملفات الدورة تتضمن ملخصات، نماذج محلولة وغير محلولة، خرائط مفاهيم، ومفردات مهمة. سيتم تزويدك بها بعد الاشتراك.',
    'الخطة': 'بعد التسجيل ستحصل على خطة مذاكرة تناسب وقتك ومستواك، ويمكنك تحميلها أو مشاركتها.',
    'التسجيل': 'للتسجيل في الدورة يرجى تعبئة النموذج في صفحة التسجيل واختيار طريقة الدفع المناسبة.',
    'السلام': 'وعليكم السلام! كيف يمكنني مساعدتك؟',
    'مرحبا': 'مرحباً بك! أنا هنا لمساعدتك في أي سؤال عن الدورة والخطط.',
    'رمضان': 'لدينا خصم خاص بمناسبة شهر رمضان وتذهب نسبة من الرسوم للأعمال الخيرية.',
    'نجوم': 'يمكنك الدفع عبر نجوم تيليجرام وسيتم إضافتك إلى القنوات بعد التحقق من الدفع.',
    'شكرا': 'العفو! نتمنى لك التوفيق في رحلتك التعليمية.',
    'help': 'اكتب كلمة مفتاحية مثل: دورة، الملفات، الخطة، التسجيل، رمضان، وسأجيبك بما يفيدك.'
  };

  // الفترة بين كل إشعار (بالملي ثانية)
  const NOTIF_INTERVAL = 45000;

  // حالة المقاعد والخصم
  let seatsLeft = 5; // العدد المعروض في الصفحة
  const discountEnd = new Date();
  discountEnd.setDate(discountEnd.getDate() + 7); // الخصم لمدة سبعة أيام من فتح الصفحة

  // عناصر DOM
  const navMenu = document.getElementById('navMenu');
  const menuToggle = document.getElementById('menuToggle');
  const shareBtn = document.getElementById('shareBtn');
  const reviewList = document.getElementById('reviewList');
  const notificationContainer = document.getElementById('notificationContainer');
  const assistantBtn = document.getElementById('assistantBtn');
  const chatWindow = document.getElementById('chatWindow');
  const chatMessages = document.getElementById('chatMessages');
  const chatInput = document.getElementById('chatInput');
  const sendBtn = document.getElementById('sendBtn');
  const seatSpan = document.getElementById('seatCount');
  const discountTimerSpan = document.getElementById('discountTimer');

  /* دوال مساعدة */

  // تفعيل/إخفاء قائمة التنقل للموبايل
  function toggleNav() {
    navMenu.classList.toggle('active');
  }

  // نسخ رابط الموقع إلى الحافظة
  function copyLink() {
    const tempInput = document.createElement('input');
    tempInput.value = window.location.href;
    document.body.appendChild(tempInput);
    tempInput.select();
    document.execCommand('copy');
    document.body.removeChild(tempInput);
    alert('تم نسخ رابط الدورة إلى الحافظة.');
  }

  // إنشاء بطاقات الآراء في الصفحة
  function renderReviews() {
    if (!reviewList) return;
    reviewList.innerHTML = '';
    reviews.forEach(item => {
      const card = document.createElement('div');
      card.className = 'review-card';
      card.innerHTML = `
        <div class="name">${item.name}</div>
        <div class="quote">${item.quote}</div>
      `;
      reviewList.appendChild(card);
    });
  }

  // إنشاء إشعار عشوائي
  function showNotification() {
    if (!notificationContainer) return;
    const name = namesPool[Math.floor(Math.random() * namesPool.length)];
    const message = notificationMessages[Math.floor(Math.random() * notificationMessages.length)];
    const notif = document.createElement('div');
    notif.className = 'notification';
    notif.innerHTML = `<strong>${name}</strong> ${message}`;
    notificationContainer.appendChild(notif);
    // إزالة بعد 5 ثوانٍ
    setTimeout(() => {
      notif.remove();
    }, 5000);
  }

  // تشغيل الإشعارات بشكل متكرر
  function startNotifications() {
    showNotification();
    setInterval(showNotification, NOTIF_INTERVAL);
  }

  // تحديث عداد المقاعد وعدّاد انتهاء الخصم
  function updateSeatAndCountdown() {
    // عرض المقاعد المتبقية
    if (seatSpan) seatSpan.textContent = seatsLeft;
    // تحديث العدّاد كل دقيقة
    function updateTimer() {
      const now = new Date();
      const diff = discountEnd - now;
      if (diff <= 0) {
        discountTimerSpan.textContent = '0';
        return;
      }
      const days = Math.ceil(diff / (1000 * 60 * 60 * 24));
      discountTimerSpan.textContent = `${days} يوم${days > 1 ? 'اً' : ''}`;
    }
    updateTimer();
    setInterval(updateTimer, 60000);
  }

  // إرسال رسالة المساعد وإظهار الرد
  function sendMessage() {
    const text = chatInput.value.trim();
    if (!text) return;
    appendMessage('user', text);
    chatInput.value = '';
    // البحث عن كلمة مفتاحية
    let response = 'عذراً، لم أفهم سؤالك. جرّب كلمات مثل دورة، الملفات، الخطة، التسجيل.';
    for (const keyword in chatResponses) {
      if (text.includes(keyword)) {
        response = chatResponses[keyword];
        break;
      }
    }
    // إضافة تأخير بسيط لمحاكاة الكتابة
    setTimeout(() => {
      appendMessage('bot', response);
    }, 600);
  }

  // إضافة رسالة إلى شاشة الدردشة
  function appendMessage(sender, text) {
    const msg = document.createElement('div');
    msg.className = 'chat-msg ' + (sender === 'user' ? 'user-msg' : 'bot-msg');
    msg.textContent = text;
    chatMessages.appendChild(msg);
    chatMessages.scrollTop = chatMessages.scrollHeight;
  }

  // تبديل إظهار النافذة
  function toggleChat() {
    chatWindow.classList.toggle('active');
  }

  /* ربط الأحداث */
  if (menuToggle) {
    menuToggle.addEventListener('click', toggleNav);
  }
  if (shareBtn) {
    shareBtn.addEventListener('click', copyLink);
  }
  if (assistantBtn) {
    assistantBtn.addEventListener('click', toggleChat);
  }
  if (sendBtn) {
    sendBtn.addEventListener('click', sendMessage);
  }
  if (chatInput) {
    chatInput.addEventListener('keypress', (e) => {
      if (e.key === 'Enter') {
        e.preventDefault();
        sendMessage();
      }
    });
  }

  // تنفيذ المهام عند تحميل الصفحة
  document.addEventListener('DOMContentLoaded', () => {
    renderReviews();
    startNotifications();
    updateSeatAndCountdown();
  });
})();